#include "countNegatives.h"

int Solution::countNegatives(std::vector<std::vector<int> >& grid){
  int m = 0, position = grid[0].size();
  for (auto row : grid) {
    for (int i = position; i >= 0; i --) {
      
    }
  }
  return 0;
}