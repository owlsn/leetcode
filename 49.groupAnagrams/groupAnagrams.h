#include <vector>
#include <string>

class Solution {
public:
  Solution() = default;
  ~Solution() = default;
  std::vector<std::vector<std::string> > groupAnagrams(std::vector<std::string>& strs);
};